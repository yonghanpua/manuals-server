<%- include("partials/head", {
  title: name,
  extraStyles: `
    body { height: 100vh; display: flex; flex-direction: column; }
    nav { border-bottom: 1px solid var(--border); padding: 0.9rem 1.25rem; display: flex; align-items: center; gap: 1rem; flex-shrink: 0; }
    nav h1 { font-size: 0.95rem; font-weight: 500; text-transform: capitalize; }
    .pdf-frame { flex: 1; width: 100%; border: none; background: #fff; }
  `
}) %>

<nav>
  <a href="/" class="back">&#8592; All Manuals</a>
  <h1><%= name %></h1>
</nav>
<iframe class="pdf-frame" src="<%= pdfUrl %>"></iframe>

</body>
</html>